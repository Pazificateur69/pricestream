# Example secret — DO NOT commit real secrets.
# Apply with: kubectl apply -f secret.example.yaml
apiVersion: v1
kind: Secret
metadata:
  name: pricestream-secret
  namespace: pricestream
type: Opaque
stringData:
  DJANGO_SECRET_KEY: "replace-me-with-a-real-secret"
  POSTGRES_PASSWORD: "replace-me"
